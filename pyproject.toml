[project]
name = "ai-agent-sandbox"
version = "0.1.0"
description = "A containerized FastAPI service for running AI agent tasks in a restricted Python sandbox."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.97.0",
    "fastapi>=0.136.1",
    "pydantic>=2.13.3",
    "uvicorn[standard]>=0.46.0",
]
